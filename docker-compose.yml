services:
  cryptobot:
    build: .
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite:////data/cryptobot.db
    volumes:
      - cryptobot-data:/data
    restart: unless-stopped

volumes:
  cryptobot-data:
